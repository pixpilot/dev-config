name: CodeQL Configuration

paths:
  - src

paths-ignore:
  - tests
  - '**/*.test.ts'
  - '**/*.spec.ts'
  - '**/*.test.tsx'
  - '**/*.spec.tsx'

query-filters:
  # Falls over on our dynamic RegExp generation
  - exclude:
      id: js/regex-injection
